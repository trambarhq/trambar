var _ = require('lodash');
var React = require('react'), PropTypes = React.PropTypes;

module.exports = React.createClass({
    displayName: 'PushNotifier',
    propTypes: {
        onNotify: PropTypes.func
    },

    triggerNotifyEvent: function() {

    },

    render: function() {

    },
});
